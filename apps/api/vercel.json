{
  "version": 2,
  "builds": [
    {
      "src": "src/lambda.ts",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 60
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/lambda.ts"
    }
  ]
}
